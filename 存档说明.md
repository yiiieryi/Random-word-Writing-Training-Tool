# 词雾 · 项目存档说明

> 存档日期：2026-09-06 · 本地词汇灵感写作工具（个人向，非后台系统）

## 这是什么

一个完全本地运行、数据保存在浏览器 IndexedDB 的写作灵感工具：
**随机双词 → 词组灵感 → 收藏 → 写作 → 保存 → 历史**，附带独立的词组管理与系统设置页。
无需后端、无需登录、无数据库服务器，刷新或关闭浏览器数据都不丢失。

## 技术栈

- React 18 + TypeScript + Vite 5 + Tailwind CSS 3
- Dexie.js（IndexedDB 封装）、react-router-dom（Hash 路由）、lucide-react（图标）
- 生产构建：`npm run build`（tsc 类型检查 + vite 打包）

## 运行方法

**最简单：双击 `启动词雾.bat`**
- 首次运行会自动安装依赖（约 1-2 分钟），之后秒开
- 自动启动服务器并打开浏览器 → http://localhost:5173
- 关闭黑色窗口即停止服务器，不依赖 AI 会话

手动方式：

```bash
cd <解压目录>
npm install     # 安装依赖（存档已排除 node_modules，需重新安装）
npm run dev     # 启动开发服务器 → http://localhost:5173
```

## 已实现功能（全部真实可交互，已通过端到端回归）

### 首页（核心体验：打开 → 随机两词 → 开始写）
- 双词卡片：左右独立随机、重新随机、一键全部重新随机
- 每张卡片可**自主选择分类**（全部/田园/城市/器物/生灵/情绪/工业/古风/天象/自定义分类）后在该分类内抽取
- 词汇与词组**去重**：同一词页内不重复出现；词组不分左右（「雾气×惆怅」与「惆怅×雾气」视为同一词组，收藏自动去重）；随机时避开最近出现过的词与搭配
- 收藏词组（无序去重）、点击收藏回填为当前词组、删除收藏
- 大型写作区：不限字数、自动增高、650ms 防抖自动保存草稿、页面切换不丢字
- **Tab 键缩进**（单行插入 / 多行批量缩进 / Shift+Tab 取消）
- 行距固定 1.75 倍、16px 字号（适合阅读的默认值）
- 保存作品：自动记录创建/修改时间，标题留空自动生成（如「雾气与惆怅」）
- 历史写作：按更新时间倒序、显示标题/时间/开头预览、点击续写、删除前确认、限制展示数量+「查看全部」
- 保存成功轻量 Toast

### 词组管理（/word-library）
- 8 个原生分类种子词库（每类 24 词，共 192 词）+ 6 个可导入的扩展分类
- 词库墙：CSS Grid 自动布局卡片，分类内词汇**多行排布 + 上下滚动**，最大化展示
- 编辑模式：双击改名（查重）、新增词汇（查重+trim）、空分类才可删除、新建分类（查重）
- **拖拽词汇到删除区删除**（拖出卡片外、Drop Zone 高亮、松开删除）
- 右下角拖拽调整板块大小，其他板块 **Grid 自动重新布局**，不重叠
- **批量导入**：文件导入（JSON/CSV）+ **文本粘贴导入** + 格式示范 + 可复制的「AI 生成提示词」模板；新分类自动创建、重复跳过、显示导入统计
- 词库导出（JSON 备份）

### 系统设置（/settings）
- 导出全部文稿（`writing-backup-<日期>.json`：ID/标题/正文/词组/创建/更新时间）
- 导入文稿（生成新 ID、不覆盖已有、按标题+内容查重、显示导入结果）

### 数据
- IndexedDB（库名 `wordspark`）：分类、词汇、收藏、文稿、草稿全部持久化
- 已验证：刷新不丢、关闭浏览器重开仍保留

## 丰富词库文件

`word-library-rich.json`：14 分类、712 词（8 个原生分类扩充至各 56 词 + 新增灯火/水泽/声音/时间/旅途/梦境各 44 词）。
导入方式：词组管理 → 批量导入 → 从文件导入（或全选复制粘贴到文本框）。

## 验证与测试

- 端到端回归：`node scripts/verify.mjs`（23 项断言，需 dev server 运行；导航等待一律 domcontentloaded）
- 截图脚本：`scripts/shot-home.mjs`、`scripts/shot-import.mjs`、`scripts/shot-scroll-toolbar.mjs`
- 已通过：生产构建（tsc + vite build）、23/23 回归、刷新/重启持久化实测

## 目录结构（src/）

- `pages/` Home（首页）、WordLibrary（词库墙+编辑模式）、Settings（备份）
- `components/` WordCards（双词卡片+分类筛选）、Collections、WritingSection、HistorySection、Header、ConfirmDialog、EmptyState
- `hooks/` useWritingDraft（草稿防抖+pagehide 落盘）、useToast
- `db/db.ts` Dexie 模型；`data/seed.ts` 种子词库；`lib/library.ts` 导入导出解析；`utils/` 格式化与下载

## 恢复继续开发

- 解压后 `npm install && npm run dev` 即可继续；
- 若沿用当前 AI 会话，直接说「恢复词雾项目（路径 …\new-chat）」即可；
- 若重开新窗口，把本说明与项目路径提供给新会话即可无缝续接。
